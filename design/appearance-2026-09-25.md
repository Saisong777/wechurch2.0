# WeChurch 明亮與暗色外觀

## 範圍

依 Sai 要求，提供「明亮」「暗色」「跟隨裝置」。沿用既有 next-themes，沒有增加套件、migration 或會員欄位。

- 桌機共用標頭、首頁、登入與後台都有圖示選單；手機沿用原本導覽選單，不增加第二排或底列。
- 個人管理的「外觀」有三選一控制項，觸控高度至少 44px。
- 預設明亮，選擇儲存於該瀏覽器 `wechurch-theme`。跨分頁同步，不等同跨裝置帳號同步。
- 跟隨裝置時回應系統顏色偏好；無法使用瀏覽器儲存時，當次仍可切換，但無法保證下次記住。
- 初始載入腳本先套用已選外觀，並同步瀏覽器 theme-color、原生表單 color-scheme。
- 深色字體、背景、輸入欄、閱讀區與舊版淺色提示改用主題配色。照片、QR、圖卡與投影內容不做整張反相。

## 驗收

- 新增 11 項測試：切換、重新掛載、儲存受限、系統偏好變更、跨分頁及初始載入保護。
- 完整 Vitest、typecheck、部署規則、隔離 HTTP／DB 完整性測試、build 及 lint（0 errors）通過。
- B 實際桌機首頁切換與重新整理保留暗色通過。
- B 390px 聖經章節與個人管理暗色可讀，跨頁保留；320px 三種模式選項不溢出，明亮與跟隨裝置選取正常。
- B 桌機 CRM 的標頭、表格、篩選與角色標籤暗色可讀。
- B 私人禱告新增表單、輸入欄與空狀態暗色可讀；沒有送出或新增測試紀錄。
- 當日教會靈修尚未發布，保留原狀，沒有為了外觀測試擅自發布課表或新增筆記。
- 已以瀏覽器尺寸驗收，不宣稱實體 iPhone／Android 全機型驗收；獨立舊版靜態遊戲／投影頁不屬主 App 外觀切換範圍。

## 發布與回復

- 僅 Railway B：`38b8a5c0-7b91-43fd-bf0f-d4222f163408`，SUCCESS。
- 來源提交：`51346ef62862248de232c39ebec0dd95f6b2b9ad`。
- 454 檔指紋：`884d4f1cfad63f7514cab31aecadefe096a399e4caaf09980fb16c278d3536b4`。
- 線上容器 manifest、健康檢查及來源提交一致；Google 邀請／state／PKCE 防護驗證通過，本次沒有重做真實 Google 同意流程。
- A 保持 `a8a4db29-527f-4cc3-8d17-caed230f69cb`，不修改資料、角色與登入憑證。
- GitHub 功能分支 `codex/light-dark-appearance`，救援標籤 `b-2026-09-25-38b8a5c0`；未合併 main／integration。
- 前版回復基準為 `b-2026-09-24-c4349339`。GitHub 只存程式與發布證據，不含 DB／密碼。
